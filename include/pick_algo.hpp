#pragma once

#include "common.hpp"

/**
 * @brief Computes area of polygon using Pick's theorem
 *
 * @param polygon Polygon to compute the area
 * @return Area of polygon
 */
ll pick_algo(Polygon_2 &polygon);
